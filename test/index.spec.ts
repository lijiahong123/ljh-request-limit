// import { asyncPool } from "../src/main.ts";

// console.log(asyncPool);

describe("asyncpool-js", () => {
  it("test limit params", () => {});
  it("test requestParams params", () => {});
  it("test useAllSettled params", () => {});
});

describe("test", () => {
  it("add", () => {
    expect(1 + 1).toBe(2);
  });
});
